{% extends 'base.html.twig' %}

{% block title %}Ajout d'article{% endblock %}

	{% block body %}
		{{ form_start(form) }}
		{{ form_label(form.title, 'titre', {'label_attr' : {'class':'ma-classe'}}) }}
		{{ form_errors(form.title) }}
		{{ form_widget(form.title, {'attr':{'class': 'form-control-lg', 'placeholder': 'libelle intéressant svp'}}) }}
		{{ form_end(form) }}

		{{form_row(form.content,
				{
					'label': 'Soyez inventif svp',
					'attr':{'class':'form-control-lg', 'placeholder':'okkkkk'}
				}
			)}}
			
	{% endblock %}